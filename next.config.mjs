/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/sitedojobteste",
  assetPrefix: "/sitedojobteste/",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;